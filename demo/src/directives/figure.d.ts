import { Directive } from './types';
export declare type Args = {
    src: string;
};
export declare type Opts = {
    name: string;
};
declare const figure: {
    figure: Directive<Args, Opts>;
};
export default figure;
